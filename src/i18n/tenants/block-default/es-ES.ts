import type { TTranslationMap } from '@/config/i18n.config';

/**
 * Block Default Spanish (Spain) overrides.
 * Currently empty — the base es-ES translations serve as the default.
 */
export const esES: TTranslationMap = {};

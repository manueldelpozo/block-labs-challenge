import type { TTranslationMap } from '@/config/i18n.config';

/**
 * Block Default Japanese overrides.
 * Currently empty — the base ja-JP translations serve as the default.
 */
export const jaJP: TTranslationMap = {};

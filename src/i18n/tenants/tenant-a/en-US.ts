import type { TTranslationMap } from '@/config/i18n.config';

/**
 * Tenant A English overrides.
 * Currently empty — the base en-US translations serve as the default.
 * Add tenant-specific terminology overrides here.
 */
export const enUS: TTranslationMap = {};
